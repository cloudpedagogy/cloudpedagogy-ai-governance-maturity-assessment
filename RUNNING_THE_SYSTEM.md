# Running the System

## View Locally
To view the application locally using a simple Python server:

```bash
python3 -m http.server 8000 --directory dist
```

Alternatively, for development:

```bash
npm install
npm run dev
```
